# Fire the "snap" trigger: app detected an anomaly, capture the snapshot.
# Usage: perfetto -c test/configs/snap_trigger.cfg --txt
# (Equivalent in-process: Tracing::ActivateTriggers({"snap"}))
activate_triggers: "snap"
